# 计算机网络考研复习指导

!!! note "小声吐槽"

    如果不是为了应付《计算机网络》课程的考试，这种书我是碰都不想碰的。《计算机网络》课程的考试极其死板，不想评价。

    仅阅读第 1、2 章。

## 第一章：计算机网络体系结构

- 电路交换、报文交换（存储 - 转发）、分组交换（包）
- Round-Trip Time
- 物理层、数据链路层、网络层、传输层、会话层、表示层、应用层
- 网络接口层、网际层、传输层、应用层

## 第二章：物理层

- 码元：一个信号波形表示 $k$ 进制数据。
- 波特率：码元传输速率。
- 比特率：信息传输速率。
- 带宽：频带宽度，为 $\mathrm{Hz}$，与 $\mathrm{b/s}$ 直接换算。
- 奈奎斯特定理：理想低通信道中，为避免码间串扰，极限码元传输速率为 $2W$，$W$ 为信道带宽。用 $V$ 表示码元离散电平数量，则极限数据率为

    $$
    2W \log_2 V \mathrm{(b/s)}
    $$

    结论：码元传输速率受限，必须使每个码元携带更多比特信息，采用多元制调制方法。

- 香农定理：带宽受限且有高斯白噪声干扰的信道的极限传输速率（无差错）

    $$
    W \log_2 (1 + \frac{S}{N}) \mathrm{(b/s)}
    $$

    $S/N$ 为信号功率与噪声功率之比，单位为 $\mathrm{dB}$。

    结论：信噪比越大速率越高；低于极限速率就有方法无差错传输。

- 编码：
    - 数字信号：
        - 归零编码 RZ：每个时钟周期中间跳变到 0 以同步时钟。
        - 非归零编码 NRZ。
        - 反向归零编码 NRZI：信号的翻转代表 1，不变代表 0。
        - 曼彻斯特编码：每个码元中间跳变。前高后低代表 1，前低后高代表 0。占频段宽度是原始基带的两倍。
    - 模拟信号：
        - 频移键控 FSK：频率不同代表不同的二进制码。
        - 相移键控 PSK：相位不同代表不同的二进制码。
        - 正交振幅调制 QAM：振幅和相位不同代表不同的二进制码。

## 第三章：数据链路层
