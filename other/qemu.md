# Proxmox VE 和 QEMU

## Proxmox VE 实践

### `qm` 命令

`qm` 命令是 Proxmox VE 的虚拟机管理命令，

## QEMU 实践

### Qemu guest agent

!!! quote

    - [Executing arbitrary commands in your libvirt/Qemu virtual machine through Qemu guest agent – 0xf8.org](https://www.0xf8.org/2022/01/executing-arbitrary-commands-in-your-libvirt-qemu-virtual-machine-through-qemu-guest-agent/)

> Meet Qemu’s guest agent. Modern Linux distributions automatically install for you it when they detect running inside a Qemu virtual machine, otherwise you might want to install it manually.
